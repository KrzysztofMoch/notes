const APP_THEMES = {
    BLUE: {
        primary: '#9ebedb',
        secondary: '#255585',
        fontColor: '#fff',
    }
}

type APP_THEMES_TYPE = 'BLUE';

export type { APP_THEMES_TYPE }
export default APP_THEMES;